export function recordReducer(state = {}, action) {
    switch (action.type) {
        case "SET_MODAL": {
            break;
        }
        case "SET_CONTEXT_MENU": {
            break;
        }
        case "REFRESH_UI": {
            break;
        }
        default:
            return state;
    }
    return newState;
}
